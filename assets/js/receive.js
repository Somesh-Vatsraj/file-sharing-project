const form=document.querySelector('#lookupForm'),err=document.querySelector('#lookupError'),card=document.querySelector('#fileCard');function bytes(n){if(n<1024)return n+' B';let u=['KB','MB','GB','TB'],i=-1;do{n/=1024;i++}while(n>=1024&&i<u.length-1);return n.toFixed(n>=100?0:1)+' '+u[i]}form?.addEventListener('submit',async e=>{e.preventDefault();err.classList.add('hidden');card.classList.add('hidden');const fd=new FormData(form);try{const r=await fetch('api_lookup.php',{method:'POST',body:fd});const d=await r.json();if(!r.ok||!d.success)throw new Error(d.message||'Code lookup failed');document.querySelector('#downloadName').textContent=d.file.name;document.querySelector('#downloadSize').textContent=bytes(d.file.size);document.querySelector('#downloadExpiry').textContent='Expires '+new Date(d.file.expires_at.replace(' ','T')).toLocaleString();document.querySelector('#downloadCount').textContent=d.file.download_count+' / '+d.file.max_downloads+' downloads';document.querySelector('#downloadBtn').href='download.php?code='+encodeURIComponent(d.file.code);card.classList.remove('hidden')}catch(x){err.textContent=x.message;err.classList.remove('hidden')}});